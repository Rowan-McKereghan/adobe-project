apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090  # Connect Grafana to Prometheus service
    isDefault: true  # Make this the default data source

